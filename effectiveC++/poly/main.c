#include "bst.h"

void printBSTArray(ostream& s,
		const BST array[],
		int numofElements)
{
	for (int i =0;i,numOfElements;++i)
		s << arra[i]
}
int main()
{
	BST array[10];
	printBSTArray(cout,array,10);
}
